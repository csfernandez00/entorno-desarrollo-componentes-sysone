import styled from "styled-components";
import COLORS from "../../common/theme/colors";
import typo from "../../common/theme/typo";

export const SectionTitle = styled(typo.H1)`
	color: ${COLORS.SECONDARY};
	margin-bottom: 1rem !important;
`;

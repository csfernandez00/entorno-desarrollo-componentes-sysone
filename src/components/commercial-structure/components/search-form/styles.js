import styled from "styled-components";
import { Button, Skeleton } from "antd";

export const LargeButton = styled(Button)`
	width: 100%;
`;

export const LabelStructureCode = styled.div`
	& label {
		margin-left: 45px !important;
	}
`;
